name: 发布新版本

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (例如: v1.0.0)'
        required: true

jobs:
  # 创建 GitHub Release
  create-release:
    name: 创建 Release
    runs-on: ubuntu-latest
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - uses: actions/checkout@v4
      
      - name: 创建 Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref_name }}
          release_name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          body: |
            ## 🎉 Neon Tools ${{ github.ref_name }}
            
            ### 新功能
            - 请在此处添加新功能说明
            
            ### 改进
            - 请在此处添加改进说明
            
            ### 修复
            - 请在此处添加 Bug 修复说明
            
            ---
            
            ### 📦 下载
            
            **Windows 用户**：下载 `neon-tools-windows-x64.exe`
            **macOS 用户**：下载 `neon-tools-macos-x64.dmg`
            **Linux 用户**：下载 `neon-tools-linux-x64.AppImage`

  # 构建 Windows 版本
  build-windows:
    name: 构建 Windows 安装包
    runs-on: windows-latest
    needs: create-release
    steps:
      - uses: actions/checkout@v4
      
      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: 安装依赖
        run: npm ci
      
      - name: 构建 Electron
        run: npm run build:electron
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: 上传到 Release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.create-release.outputs.upload_url }}
          asset_path: ./dist-electron/neon-tools-setup.exe
          asset_name: neon-tools-windows-x64.exe
          asset_content_type: application/octet-stream

  # 构建 macOS 版本
  build-macos:
    name: 构建 macOS 安装包
    runs-on: macos-latest
    needs: create-release
    steps:
      - uses: actions/checkout@v4
      
      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: 安装依赖
        run: npm ci
      
      - name: 构建 Electron
        run: npm run build:electron
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false
      
      - name: 查找 DMG 文件
        id: find_dmg
        run: |
          DMG_PATH=$(find dist-electron -name "*.dmg" | head -n 1)
          echo "dmg_path=$DMG_PATH" >> $GITHUB_OUTPUT
      
      - name: 上传到 Release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.create-release.outputs.upload_url }}
          asset_path: ${{ steps.find_dmg.outputs.dmg_path }}
          asset_name: neon-tools-macos-x64.dmg
          asset_content_type: application/octet-stream

  # 构建 Linux 版本
  build-linux:
    name: 构建 Linux 安装包
    runs-on: ubuntu-latest
    needs: create-release
    steps:
      - uses: actions/checkout@v4
      
      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: 安装依赖
        run: npm ci
      
      - name: 构建 Electron
        run: npm run build:electron
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: 查找 AppImage 文件
        id: find_appimage
        run: |
          APPIMAGE_PATH=$(find dist-electron -name "*.AppImage" | head -n 1)
          echo "appimage_path=$APPIMAGE_PATH" >> $GITHUB_OUTPUT
      
      - name: 上传到 Release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.create-release.outputs.upload_url }}
          asset_path: ${{ steps.find_appimage.outputs.appimage_path }}
          asset_name: neon-tools-linux-x64.AppImage
          asset_content_type: application/octet-stream

