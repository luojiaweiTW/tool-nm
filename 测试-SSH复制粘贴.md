# 🧪 SSH 工具复制粘贴功能测试指南

## ✅ 已修复的问题
**问题：** 在 SSH 终端中按 `Ctrl+Shift+C` 无法复制选中的文本

**原因：** 键盘事件监听器绑定在外层容器，无法拦截终端内部的按键

**修复：** 使用 `xterm.attachCustomKeyEventHandler()` 正确拦截终端内的快捷键

---

## 🚀 快速测试步骤

### 1️⃣ 启动应用
```bash
npm run dev
```
访问：http://localhost:5173/#/tools/ssh

### 2️⃣ 连接 SSH 服务器
- 点击 **"新建连接"** 按钮
- 填入你的 SSH 服务器信息：
  - 主机地址（如：`192.168.1.100`）
  - 端口（默认：`22`）
  - 用户名（如：`root`）
  - 密码或密钥文件
- 点击 **"连接"** 按钮

### 3️⃣ 测试复制功能（Ctrl+Shift+C）

#### 步骤：
1. 在终端中执行命令（如：`ls -la` 或 `pwd`）
2. **用鼠标选中** 终端中的输出文本（会有蓝色高亮）
3. 按下 **`Ctrl+Shift+C`**

#### 预期结果：
- ✅ 页面右上角出现绿色提示：**"已复制到剪贴板"**
- ✅ 控制台输出：`✓ Text copied to clipboard: [内容]`
- ✅ 打开记事本按 `Ctrl+V`，能粘贴刚才复制的内容

### 4️⃣ 测试粘贴功能（Ctrl+Shift+V）

#### 步骤：
1. 在其他地方复制一段文本（如记事本中复制：`echo "Hello World"`）
2. 回到 SSH 终端窗口
3. 按下 **`Ctrl+Shift+V`**

#### 预期结果：
- ✅ 页面右上角出现绿色提示：**"已粘贴"**
- ✅ 控制台输出：`✓ Text pasted from clipboard: [内容]`
- ✅ 终端中出现粘贴的文本（如：`echo "Hello World"`）
- ✅ 按回车执行命令

---

## 🎯 测试检查清单

### 功能测试
- [ ] 能用鼠标选中终端中的文本（有蓝色高亮）
- [ ] 按 `Ctrl+Shift+C` 能复制选中的文本
- [ ] 复制后显示 **"已复制到剪贴板"** 提示
- [ ] 复制的内容能在其他应用（如记事本）中粘贴
- [ ] 按 `Ctrl+Shift+V` 能粘贴剪贴板内容
- [ ] 粘贴后显示 **"已粘贴"** 提示
- [ ] 粘贴的内容正确出现在终端中

### 边界情况测试
- [ ] 未选中文本时按 `Ctrl+Shift+C`：不应有报错
- [ ] 剪贴板为空时按 `Ctrl+Shift+V`：不应有报错
- [ ] 复制多行文本：应完整保留换行符
- [ ] 复制包含特殊字符的文本（如：`$ > | &`）：应完整复制
- [ ] 连续多次复制粘贴：功能正常

### UI/UX 检查
- [ ] 提示消息位置合理（右上角）
- [ ] 提示消息自动消失（3秒后）
- [ ] 提示消息颜色正确（成功=绿色，失败=红色）
- [ ] 快捷键提示清晰可见（页面顶部工具栏）

---

## 🐛 如果功能不工作，检查：

### 1. 浏览器权限
- 确保浏览器允许访问剪贴板
- Chrome/Edge：点击地址栏左侧的 🔒 图标 → 权限 → 剪贴板 → 允许
- Firefox：自动允许（无需设置）

### 2. 开发者控制台
按 `F12` 打开控制台，查看是否有错误信息：
```javascript
// 成功时应该看到：
✓ Text copied to clipboard: [内容]
✓ Text pasted from clipboard: [内容]

// 失败时可能看到：
Failed to copy: [错误信息]
Failed to paste: [错误信息]
```

### 3. 选中文本是否高亮
- 如果文本没有蓝色高亮，说明没有正确选中
- 重新用鼠标拖动选中文本

### 4. 是否在 HTTPS 或 localhost
- Clipboard API 只能在安全上下文中使用
- 确保访问的是 `localhost` 或 `https://`

---

## 📊 预期测试结果

```
======================================================================
📊 SSH 复制粘贴功能测试报告
======================================================================
🔗 测试页面: /tools/ssh
📡 测试状态: ✅ 通过

💬 测试项目:
   1. ✅ 文本选中功能
   2. ✅ Ctrl+Shift+C 复制
   3. ✅ 复制成功提示
   4. ✅ 剪贴板内容正确
   5. ✅ Ctrl+Shift+V 粘贴
   6. ✅ 粘贴成功提示
   7. ✅ 终端显示粘贴内容
   8. ✅ 多行文本复制
   9. ✅ 特殊字符复制

📊 统计:
   - 测试用例: 9 个
   - 通过: 9 个
   - 失败: 0 个

✅ 结论: 功能完全可用，修复成功！
======================================================================
```

---

## 💡 技术说明

### 修复前（错误）
```javascript
// ❌ 监听外层容器，无法拦截终端内部按键
terminalContainer.value?.addEventListener('keydown', handleKeyboard)
```

### 修复后（正确）
```javascript
// ✅ 使用 xterm 的自定义键盘处理器，正确拦截终端按键
xterm.attachCustomKeyEventHandler((event: KeyboardEvent) => {
  if (event.ctrlKey && event.shiftKey && event.key.toLowerCase() === 'c') {
    // 复制逻辑
    return false // 阻止 xterm 默认处理
  }
  if (event.ctrlKey && event.shiftKey && event.key.toLowerCase() === 'v') {
    // 粘贴逻辑
    return false // 阻止 xterm 默认处理
  }
  return true // 其他按键正常处理
})
```

---

## 📝 反馈

如果测试过程中发现问题，请提供以下信息：
1. 浏览器类型和版本
2. 是否显示错误提示
3. 控制台的错误信息（按 F12 查看）
4. 操作步骤的详细描述

---

**文档创建时间：** 2025-11-05  
**功能状态：** ✅ 已修复，待测试验证








